[2026-07-13 18:10] major: Rename the `placeholder` icon to `circle` (both `base` and `fill` variants). The glyph is unchanged (a circle) — only the name changes. Regenerated catalog/index/source: `iconNames`/`iconVariants` now list `circle` (not `placeholder`), and the component exports are `CircleBaseIcon`/`CircleFillIcon` (were `PlaceholderBaseIcon`/`PlaceholderFillIcon`). Migrate `getIcon("placeholder")` -> `getIcon("circle")` and the `Placeholder*Icon` imports -> `Circle*Icon`.
[2026-07-14 10:00] minor: Add laptop, mail, and tablet icons (base + fill variants).
[2026-07-14 10:00] patch: Fix duplicate/leftover paths in location, attention, camera, desktop — the AI sources re-painted shapes (nudged history copies; a stroked copy left in a fill file). nice-svg-generator now drops overlapping near-duplicate paths and (for fill variants) non-fill paths, so these icons regenerate as a single clean copy. Also add a README with the full icon table.
[2026-07-17 11:57] minor: Add encoded icon surface: getIconEncoded/getIconEncodedKey (nice-icons/get-icon-encoded) + base64 data-URI set (nice-icons/base64, per-icon base64.json), color baked base/#000 + night/#fff. Add watch icon; refresh file glyph.
