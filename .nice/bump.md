[2026-07-13 18:10] major: Rename the `placeholder` icon to `circle` (both `base` and `fill` variants). The glyph is unchanged (a circle) — only the name changes. Regenerated catalog/index/source: `iconNames`/`iconVariants` now list `circle` (not `placeholder`), and the component exports are `CircleBaseIcon`/`CircleFillIcon` (were `PlaceholderBaseIcon`/`PlaceholderFillIcon`). Migrate `getIcon("placeholder")` -> `getIcon("circle")` and the `Placeholder*Icon` imports -> `Circle*Icon`.
