[2026-06-10 14:58] major: Split arrow and carat into four directional icons each — arrow-top/right/bottom/left and carat-top/right/bottom/left (rotated about centre). Removes the single arrow/carat; exports renamed (ArrowStrokeIcon → ArrowRightStrokeIcon, etc.)
[2026-06-22 17:36] minor: Add "copy" icon (stroke + fill). Exports CopyStrokeIcon / CopyFillIcon and adds "copy" to iconNames
[2026-06-24 19:26] minor: Normalize all icon SVGs via a config-driven scrubber (scripts/scrubSvg.js + scripts/svgStyle.config.js), run on build and at --watch startup, plus on each live .svg save (idempotent, so Illustrator saves are cleaned without a rewrite loop). Strips Adobe artifacts (generator comment, <defs><style>, .st* classes, id="Layer_1") and applies semantic classes (no-fill/fill/stroke) with currentColor. Standalone <img> of stroke icons now renders (was invisible); fill icons use currentColor instead of a hardcoded near-black. Exports/iconNames unchanged.
[2026-06-24 21:26] minor: Add "download" icon (stroke + fill). Exports DownloadStrokeIcon / DownloadFillIcon and adds "download" to iconNames
[2026-07-03 15:30] minor: Add --convert [path] to the build (generateIndex.js). Converts .source .ai files into icon SVGs via nice-svg-generator (no external pdf2svg/poppler dependency) before scrubbing/generating; incremental (only when the .svg is missing or older). Optional .source path scopes it — a folder (brands, brands/github) or a single .ai file (brands/github/fill.ai); omit to convert all of .source.
[2026-07-04 03:23] minor: Restructure to a nice-styles-style src/ layout — .ai sources under src/source/{category}/{icon}/, the generated SVG set + index under src/generated/ (now the published surface, files: ["src/generated"]). package.json main/exports/files repoint there (. → index.js, ./catalog → catalog.js, ./* → raw SVG subpaths); consumer import specifiers ("nice-icons", "nice-icons/catalog") are unchanged. generateIndex/convertAi walk the new paths and never delete SVGs. Add the first .ai-sourced base icon, lightbulb (52 icons total).
